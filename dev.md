## Repo init
Run in container once (just after initial image built):

- `bundle install`
- `git config core.hooksPath .githooks/`

## How to update content

- Create post in `_posts`, put pics into `pics`
- Preview locally with `jekyll serve`
- Commit and push — the rest will be generated